# The Reclaim Scheduling Link embed URL. Get this from Reclaim:
# Scheduling Link card → ⋮ → Share → copy the iframe `src` attribute.
# When unset, /book renders an "email aaron@hellosavvy.design" fallback.
NEXT_PUBLIC_RECLAIM_EMBED_URL=
